{
  "name": "my-portfolio-root",
  "private": true,
  "version": "1.0.0",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "postinstall": "cd client && npm ci --include=dev && cd ../server && npm ci",
    "build": "cd client && npm ci --include=dev && npm run build",
    "start": "cd server && npm ci --production && npm start",
    "heroku-postbuild": "npm run build"
  }
}
